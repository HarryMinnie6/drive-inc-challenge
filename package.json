{
  "name": "drive-inc-challenge",
  "version": "1.0.0",
  "description": "Coding challenge",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,yellow\" \"cd server && npm run start\" \"cd client && npm run dev\"",
    "test": "npm test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
